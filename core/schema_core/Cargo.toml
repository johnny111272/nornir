[package]
name = "schema_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
serde_json = { workspace = true }
jsonschema = { workspace = true }
error_core = { path = "../error_core" }
