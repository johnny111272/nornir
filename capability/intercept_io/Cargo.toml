[package]
name = "intercept_io"
version = "0.1.0"
edition = "2021"

[lib]
name = "intercept_io"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { workspace = true }
serde_json = { workspace = true }
format_core = { path = "../../core/format_core" }
